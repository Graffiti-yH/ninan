package com.denser.june.core.domain.model.enums

import com.denser.june.core.R

enum class MapTheme(val stringRes: Int) {
    APP(R.string.app_follow_theme),
    LIGHT(R.string.light),
    DARK(R.string.dark)
}
